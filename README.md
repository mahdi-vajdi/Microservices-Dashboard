Create env for each service based on the env.example in the services root folder.
Application can be built and run using docker compose tool.

## Running the app in dev environment

```bash
# linux
$ docker compose up --build -V
```
